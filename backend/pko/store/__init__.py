"""Персистентное хранилище PKO: продукты и снимки их проверок (Progress Mode).

Только `pko serve` (веб-путь) пользуется этим пакетом — офлайн `pko progress`
как был разовым файловым отчётом без истории, так и остался, ему тут нечего
делать. SQLite (stdlib `sqlite3`) — не отдельный сервис, файл на диске рядом
с `~/.pko/llm-cache`, тем же принципом простоты, что и остальной PKO.
"""

from __future__ import annotations
