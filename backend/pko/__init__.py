"""PKO progress — сравнение PPTX-плана команды с кодом репозитория.

Конвейер: PPTX (карточки задач, таймлайн) → LLM-роль planner → пункты плана
→ read-only чтение целевого репозитория → детерминированные экстракторы →
LLM-роль matcher (вердикт + evidence на пункт) → проверка evidence по коду →
отчёт о прогрессе (что сделано, что частично, что сделано сверх плана, что
осталось).

Каждая evidence-ссылка на код проверяется структурно (`pko.progress.verify`)
до попадания в отчёт: неподтверждённая не отбрасывается, но публикуется как
непроверенная, а не как «код показывает».
"""

__version__ = "0.1.0"
