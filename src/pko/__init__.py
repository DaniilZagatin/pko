"""PKO — восстановление паспортов автономного процесса по коду репозитория.

Конвейер: клон по SSH → read-only чтение истории → детерминированные экстракторы
→ кандидаты → PKO-модель → валидация кодом → BASIC Gate → отчёты.

Роли языковых моделей (обе опциональны):
  * assembler (GLM) — группирует кандидатов в BBB, возвращает только JSON;
  * writer (DeepSeek) — пишет текст отчёта по готовой модели, кода не видит.
Корректность проверяет не модель, а `pko.checks.validator` и `pko.report.guard`.
"""

__version__ = "0.1.0"

STANDARD_VERSION = "1.1"
