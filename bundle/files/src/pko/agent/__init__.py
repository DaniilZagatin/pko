"""Агент разведки: читает репозиторий сам, а не полагается на набор экстракторов.

Разделение ролей осталось прежним и в этом весь смысл: агент **предлагает**, код
**проверяет**. Любой его факт перечитывается по `path:line` на том же коммите
(`pko.agent.verify`), а статусы проверок и вердикт Gate по-прежнему считает
детерминированный код. Ни один вывод агента не может выдать допуск.

Инструменты работают поверх `GitRepo` и привязаны к одному `sha`. Bash здесь
намеренно нет: allowlist подкоманд в `pko.git.repo` — то, чем доказывается
«только чтение», и терять это доказательство ради удобства нельзя.
"""

from pko.agent.loop import AgentResult, run_scout  # noqa: F401
from pko.agent.tools import ToolBox  # noqa: F401
from pko.agent.trace import Trace, TraceStep  # noqa: F401
